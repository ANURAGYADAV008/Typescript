## TypeScript Stup
- npm install -g typescript
- npm init -y
- npx tsc --init (node package xeucatable)
# To run
- type tsc -b (that created a js file)
- type node a.js (to run js file here a is js file name which is automatic created by command type tsc -b)
